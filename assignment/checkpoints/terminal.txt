# Type your responses on a new line

# 1 - What is the command for listing the files in the current folder (directory)?
ls
# 2 - What is the command for changing directories?
cd
# 3 - What is the command for going "up" a folder (to a parent directory)?
cd ..
# 4 - What is a quick way to create a new folder?
mkdir new-folder
# 5 - What output would you expect when you run the command 'ls' in an empty folder? What about a folder containing files?
5a: Expect to see no files listed.  You will remain in the same empty folder.
5b: The files within that directory will be listed in alphabetical order
# 6 - What is a quick way to create a new file?
touch new-file-name.js (after you name the file, you designate what type of file it is by typing a "." and then including the type of file you want. Example: The types I used in my assignment were .js .css .html .txt and .jpeg)
# 7 - Run 'git --version' in your terminal. What is the output?
git version 2.15.0
# 8 - Why would you run the command 'git status'?
To see the status of files inside a specific folder.  This is detect modified files that have not been saved.
# 9 - What does the command 'git add .' do?
Add everything that is in get to the current folder.
# 10 - What is the command to push your committed changes to your repo on GitHub?
git commit -m "new-file.text"  (in the '', detail your changes for specific file)
